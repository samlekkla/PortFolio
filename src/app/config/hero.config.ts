export type HeroConfig = {
  headline: string;
  subheadline: string;
  ctaLabels: string[];
};

const heroConfig: HeroConfig = {
  headline: "Welcome to My Portfolio",
  subheadline: "Showcasing my projects, skills, and experience",
  ctaLabels: ["View Projects", "Contact Me"],
};

export default heroConfig;
